# WebSocket URL — defaults to ws://localhost:8000/ws/live if not set
# Change this to your deployed backend host in production
VITE_WS_URL=ws://localhost:8000/ws/live

# REST API base URL — defaults to http://localhost:8000 if not set
# Change this to your deployed backend host in production
VITE_API_URL=http://localhost:8000
